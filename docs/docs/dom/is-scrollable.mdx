# `isScrollable`

export const meta = {
  title: 'isScrollable',
  category: 'DOM',
  menuPriority: 100,
};

Checks that element is scrollable.

```ts
import { isScrollable } from '@krutoo/utils';

const element = document.querySelector('#something');

isScrollable(element); // boolean
```
