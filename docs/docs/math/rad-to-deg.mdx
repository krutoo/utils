# `radToDeg`

export const meta = {
  title: 'radToDeg',
  category: 'Math',
};

Takes amount of radians and returns same angle in degrees.

```ts
import { radToDeg } from '@krutoo/utils';

const degrees = radToDeg(1.2);
```
