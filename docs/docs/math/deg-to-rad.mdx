# `degToRad`

export const meta = {
  title: 'degToRad',
  category: 'Math',
};

Takes amount of degrees and returns same angle in radians.

```ts
import { degToRad } from '@krutoo/utils';

const radians = degToRad(45);
```
