# Math utils

export const meta = {
  title: 'Overview',
  category: 'Math',
  menuPriority: 1000,
};

All utils specific to the math and geometry available from package root.

```tsx
import { clamp, lerp, randomFloat } from '@krutoo/utils';
```
