# Math utils

export const meta = {
  title: '',
  category: 'Math',
};

All utilities specific to the math and geometry available from package root.

```ts
import { clamp, lerp, randomFloat } from '@krutoo/utils';
```
