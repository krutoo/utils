# React utils

export const meta = {
  title: 'Overview',
  category: 'React',
  menuPriority: 1000,
};

All [React](https://react.dev/) specific utils available from `/react` pathname.

React utils including components and hooks. All components and hooks are ready for SSR.

```tsx
import { Portal, useStableCallback, mergeRefs } from '@krutoo/utils/react';
```
