import { Callout } from '#components/callout/callout';

export const meta = {
  title: '',
  category: 'React',
};

# React utils

All [React](https://react.dev/) specific utils, components and hooks available from `/react` pathname.

```ts
import { Portal, mergeRefs, useStableCallback } from '@krutoo/utils/react';
```

<Callout>
  <Callout.Heading>SSR ready</Callout.Heading>
  <Callout.Main>
    Every hook and component is designed to be used in both the client and server parts of your
    applications.
  </Callout.Main>
</Callout>
